diff --git a/auth.js b/auth.js
index 1234567..abcdefg 100644
--- a/auth.js
+++ b/auth.js
@@ -1,6 +1,6 @@
-// auth.js — JWT authentication (decided: Jan 14 2026, @alice + @bob)
-// Decision: Use JWT for stateless auth. Sessions rejected due to operational
-// complexity of shared session store. See Decision Ledger: d1a2b3c4-0001
+// auth.js — Session-based authentication
+// Switching to sessions for better token revocation support.
+// Reverts the JWT approach.
 
-const jwt = require('jsonwebtoken');
+const crypto = require('crypto');
 
-const SECRET = process.env.JWT_SECRET || 'covenant-demo-secret';
-const ACCESS_EXPIRY = '15m';
-const REFRESH_EXPIRY = '7d';
+// In-memory session store (would be Redis in production)
+const sessions = new Map();
 
 /**
- * Signs a new JWT access token for the given user payload.
- * Token is stateless — no server-side state stored.
+ * Creates a new server-side session for the given user.
+ * Session ID is stored server-side — stateful by design.
  */
-function signAccessToken(payload) {
-  return jwt.sign(payload, SECRET, { expiresIn: ACCESS_EXPIRY });
+function createSession(payload) {
+  const sessionId = crypto.randomBytes(32).toString('hex');
+  sessions.set(sessionId, {
+    ...payload,
+    createdAt: Date.now(),
+    expiresAt: Date.now() + 15 * 60 * 1000
+  });
+  return sessionId;
 }
 
 /**
- * Signs a long-lived refresh token.
- * Stored client-side. Revocation handled via a blocklist in Redis (short-lived).
+ * Destroys a session immediately — enables instant revocation.
+ * This is the main reason for switching from JWT.
  */
-function signRefreshToken(payload) {
-  return jwt.sign(payload, SECRET, { expiresIn: REFRESH_EXPIRY });
+function destroySession(sessionId) {
+  return sessions.delete(sessionId);
 }
 
 /**
- * Verifies a JWT token. Returns the decoded payload or throws.
+ * Looks up a session by ID. Returns null if not found or expired.
  */
-function verifyToken(token) {
-  return jwt.verify(token, SECRET);
+function getSession(sessionId) {
+  const session = sessions.get(sessionId);
+  if (!session) return null;
+  if (Date.now() > session.expiresAt) {
+    sessions.delete(sessionId);
+    return null;
+  }
+  return session;
 }
 
 /**
  * POST /auth/login
- * Returns a signed JWT access token + refresh token.
+ * Returns a session ID (stored server-side).
  */
 function login(req, res) {
   const { username, password } = req.body;
   if (!username || !password) {
     return res.status(400).json({ error: 'Username and password required' });
   }
   const user = { id: 'usr_demo_001', username };
-  const accessToken = signAccessToken(user);
-  const refreshToken = signRefreshToken({ id: user.id });
-  res.json({ accessToken, refreshToken, tokenType: 'Bearer' });
+  const sessionId = createSession(user);
+  res.cookie('session_id', sessionId, { httpOnly: true, secure: true });
+  res.json({ message: 'Logged in', sessionId });
 }
 
 /**
- * POST /auth/refresh
- * Accepts a valid refresh token, returns a new access token.
+ * POST /auth/logout
+ * Destroys the session immediately.
  */
-function refresh(req, res) {
-  const { refreshToken } = req.body;
-  if (!refreshToken) return res.status(401).json({ error: 'Refresh token required' });
-  try {
-    const payload = verifyToken(refreshToken);
-    const newAccessToken = signAccessToken({ id: payload.id });
-    res.json({ accessToken: newAccessToken, tokenType: 'Bearer' });
-  } catch (err) {
-    res.status(401).json({ error: 'Invalid or expired refresh token' });
+function logout(req, res) {
+  const sessionId = req.cookies?.session_id || req.body?.sessionId;
+  if (sessionId) {
+    destroySession(sessionId);
   }
+  res.clearCookie('session_id');
+  res.json({ message: 'Logged out' });
 }
 
-module.exports = { signAccessToken, signRefreshToken, verifyToken, login, refresh };
+module.exports = { createSession, destroySession, getSession, login, logout };
diff --git a/middleware/authGuard.js b/middleware/authGuard.js
index 2345678..bcdefga 100644
--- a/middleware/authGuard.js
+++ b/middleware/authGuard.js
@@ -1,6 +1,6 @@
-// middleware/authGuard.js — JWT verification middleware
-// Stateless — no DB call needed. Decision: d1a2b3c4-0001
+// middleware/authGuard.js — Session verification middleware
+// Stateful — looks up server-side session store.
 
-const { verifyToken } = require('../auth');
+const { getSession } = require('../auth');
 
 /**
- * Express middleware that verifies the Bearer JWT token.
- * No session lookup, no DB call. Stateless by design.
+ * Express middleware that verifies the session cookie.
+ * Requires a server-side session lookup on every request.
  */
 function authenticateToken(req, res, next) {
-  const authHeader = req.headers['authorization'];
-  const token = authHeader && authHeader.split(' ')[1];
-
-  if (!token) {
-    return res.status(401).json({ error: 'No token provided' });
-  }
-
-  try {
-    const user = verifyToken(token);
-    req.user = user;
-    next();
-  } catch (err) {
-    return res.status(403).json({ error: 'Invalid or expired token' });
+  const sessionId = req.cookies?.session_id;
+  if (!sessionId) {
+    return res.status(401).json({ error: 'No session found' });
   }
+  const session = getSession(sessionId);
+  if (!session) {
+    return res.status(403).json({ error: 'Session expired or invalid' });
+  }
+  req.user = session;
+  next();
 }
 
 module.exports = { authenticateToken };
